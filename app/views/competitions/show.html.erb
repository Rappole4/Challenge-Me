<p id="notice"><%= notice %></p>

<%= render case @competition.challenge_id
		when 1 then 'push'
		when 2 then 'run'
		when 3 then 'pull'
	 	else 'pull'
	 end %>
<% if @competition.user_id == current_user.id %>
<%= link_to 'Edit', edit_competition_path(@competition) %> |
<% end %>
<%= link_to 'Back', competitions_path %>
